
jQuery(document).ready(function($) {
    /*var addressPicker = new AddressPicker({autocompleteService: {types: ['(cities)'], componentRestrictions: {country: 'ES'}}});


    $('#from').typeahead(null, {
        displayKey: 'description',
        source: addressPicker.ttAdapter()
    });
    addressPicker.bindDefaultTypeaheadEvent($('#from'));
    $(addressPicker).on('addresspicker:selected', function (event, result) {
        var $data = result.lat()+'_shb_'+result.lng()+'_shb_'+result.address();
        $('#route_from').val($data);
    })

    var _addressPicker = new AddressPicker({autocompleteService: {types: ['(cities)'], componentRestrictions: {country: 'ES'}}});

    $('#to').typeahead(null, {
        displayKey: 'description',
        source: _addressPicker.ttAdapter()
    });
    _addressPicker.bindDefaultTypeaheadEvent($('#to'));
    $(_addressPicker).on('addresspicker:selected', function (event, result) {
        var $data = result.lat()+'_shb_'+result.lng()+'_shb_'+result.address();
        $('#route_to').val($data);
    })*/
});
/*var addressPickerFrom = new AddressPicker();
 var addressPickerTo = new AddressPicker();
 addressPickerFrom.bindDefaultTypeaheadEvent($('#from'));
 $('#from').typeahead(null, {
 displayKey: 'description',
 source: addressPickerFrom.ttAdapter()
 });
 $(addressPickerFrom).on('addresspicker:selected', function (event, result) {
 $('#from_lat').val(result.lat());
 $('#from_lng').val(result.lng());
 $('#from_city').val(result.nameForType('locality'));
 });

 $('#to').typeahead(null, {
 displayKey: 'description',
 source: addressPickerTo.ttAdapter()
 });
 addressPickerTo.bindDefaultTypeaheadEvent($('#to'));
 $(addressPickerTo).on('addresspicker:selected', function (event, result) {
 $('#to_lat').val(result.lat());
 $('#to_lng').val(result.lng());
 $('#to_city').val(result.nameForType('locality'));
 });*/
